# ============================================================
#  yedam-website — 공개 저장소입니다 (GitHub Pages)
#  여기에 올라가는 것은 전부 인터넷에 공개됩니다.
#  개인정보·백업·비밀값이 절대 커밋되지 않도록 막습니다.
# ============================================================

# 서버 백업 일체 (학생 개인정보·DB 덤프·함수 소스 포함)
_backup/
_pgtools/
netlify-data/

# IDC 자체 서버로 올릴 코드. 공개될 이유가 없고,
# .env 가 같은 폴더에 놓이므로 통째로 막는다.
_server/

# 내부 작업 문서. 서버 IP·경로·보안 구성·미조치 취약점이 적혀 있다.
#
# 실측(2026-08-08): 이 폴더가 추적되고 있어서 사이트에 그대로 배포됐고
#   https://yedam.kr/_docs/서버작업_기록_20260808.md  → 200
# 으로 누구나 열 수 있었다. "취약점 목록을 공개해 둔" 셈이었다.
# 문서는 서버(C:\yedam\docs)에만 둔다.
_docs/

# DB 덤프·아카이브
*.dump
*.sql
*.sql.gz

# 로컬 작업용 스크립트·로그
_pia_deploy*.bat
_supabase_*.bat
_supabase_*.ps1
_netlify_*.bat
_netlify_*.ps1
_deploy*.txt
_kioskroot/
_dump_log.txt
_export_log.txt
_fix_log.txt

# 비밀값
.env
.env.*
*.key
*.pem
secrets.json

# OS·편집기 부산물
Thumbs.db
desktop.ini
.DS_Store
*.swp
*~
.vscode/
.idea/
